linters:
  enable:
    - gofmt
    - goimports
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unused
  disable:
    - errcheck

issues:
  exclude-rules:
    - linters:
        - revive
      text: "unused-parameter"

run:
  timeout: 3m
