---
- name: "Install required packages on {{ ansible_distribution }}"
  package:
    name: "{{ item }}"
    state: latest
  with_items:
      - unzip
      - tar
      - apt-transport-https
      - libseccomp2
      - util-linux
